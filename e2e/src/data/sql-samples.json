{
  "simple": [
    "SELECT * FROM users WHERE id = 1",
    "SELECT name, email FROM users WHERE status = 'active'"
  ],
  "complex": [
    "SELECT u.name, COUNT(o.id) as order_count FROM users u LEFT JOIN orders o ON u.id = o.user_id GROUP BY u.id HAVING order_count > 5",
    "SELECT * FROM orders WHERE created_at BETWEEN '2024-01-01' AND '2024-12-31' AND status IN ('completed', 'processing') ORDER BY created_at DESC LIMIT 100"
  ],
  "slow": [
    "SELECT * FROM users WHERE email LIKE '%@example.com%'",
    "SELECT * FROM orders WHERE DATE(created_at) = '2024-01-01'"
  ],
  "explain": [
    "EXPLAIN SELECT * FROM users WHERE email = 'test@example.com'",
    "EXPLAIN SELECT u.*, o.* FROM users u JOIN orders o ON u.id = o.user_id WHERE u.status = 'active'"
  ]
}
